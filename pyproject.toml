[project]
name = "ai"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[dependency-groups]
dev = [
    "ruff>=0.15.18",
]

[tool.ruff]
target-version = "py314"
line-length = 120

[tool.uv.workspace]
members = [
    "images/sandbox/open-design-scripts",
]
